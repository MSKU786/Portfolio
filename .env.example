# Required for the "Ask this résumé" assistant (/api/chat).
# Without it the site still builds and renders; the chat returns a 503 and the
# panel shows a "not configured on this deployment" message.
# Create a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...
